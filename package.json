{
  "name": "testnet-airdrop-hardhat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "lint": "echo \"No linter configured\"",
    "dev:node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network sepolia",
    "generate:merkle": "node scripts/generate-merkle.mjs",
    "fund:distributor": "hardhat run scripts/fund-distributor.js --network sepolia",
    "claim:demo": "node scripts/claim-demo.mjs"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.10",
    "dotenv": "^16.4.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "ethers": "^6.13.1",
    "merkletreejs": "^0.4.0",
    "keccak256": "^1.0.6"
  }
}
